# Contract tests

- JSON schemas parse and validate examples.
- OpenAPI parses and required paths exist.
- Protobuf compiles with Buf/protoc.
- Event catalog types validate against event schema.
- Kafka default topic naming and override behavior are deterministic.
- Config interpolation rejects arbitrary expressions and redacts secrets.
