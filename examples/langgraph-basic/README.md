# Basic LangGraph acceptance application

Implement a deterministic graph without an external model:

- state contains `messages` and `count`,
- one node appends an AI message,
- exported as both compiled graph and factory,
- tests config/context injection and state persistence.
