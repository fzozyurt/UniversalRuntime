[project]
name = "phase1-agent"
version = "0.1.0"
requires-python = ">=3.12,<3.14"
dependencies = ["langgraph>=1.0", "langchain-core>=1.0", "fastapi>=0.115"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
